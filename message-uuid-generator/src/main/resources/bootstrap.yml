spring:
  cloud:
    profiles: docker
    consul:
      host: localhost
      port: 8500
      discovery:
        instance_id: ${spring.application.name}:${random.int}
        prefer-ip-address: true
        healthCheckInterval: 15s
