spring:
  cloud:
    profiles: docker
    consul:
      host: consul
      port: 8500
      discovery:
        instance_id: ${spring.application.name}:${random.int}
        register: true
        prefer-ip-address: true
